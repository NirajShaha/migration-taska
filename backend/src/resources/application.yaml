spring:
  application:
    name: Screen-1

  datasource:
    url: jdbc:mysql://localhost:3306/screen1?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: manager

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update

server:
  port: 8081

jwt:
  secret-key: KPndLPZQDTKajdBikzJnQ1iSg21tPHEF
  expiration: 3600000
  refresh-expiration: 604800000

logging:
  level:
    root: INFO
    com.wvta: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
